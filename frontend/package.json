{
  "name": "testing",
  "version": "0.1.0",
  "description": "testing with an initialized project",
  "author": "ORIF Pomy Intersection",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "dev": "esbuild index.js --bundle --outfile=public/build.js --format=esm --watch --serve --servedir=public",
    "build": "esbuild index.js --bundle --format=esm --minify --outfile=public/build.js"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
